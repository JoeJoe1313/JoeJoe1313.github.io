name: Pelican CI

on:
  push:
    branches:
      - master

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Build and Deploy
        uses: ./.github/actions
        env:
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
